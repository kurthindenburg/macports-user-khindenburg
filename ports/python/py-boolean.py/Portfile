# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.0
PortGroup           github 1.0

github.setup        bastikr boolean.py 4.0 v
name                py-boolean.py
revision            0

platforms           darwin
supported_archs     noarch
license             BSD
maintainers         {khindenburg @kurthindenburg} openmaintainer

description         A small library implementing a boolean algebra
long_description    ${description}. It \
    defines two base elements, TRUE and FALSE, and a Symbol class that can \
    take on one of these two values.

checksums           rmd160  773883e670037f452ff19df84fbe468a5cb1bb7f \
                    sha256  4f821d2911a32cd33293c743cadf5528de7cfaef075bf9a88ac8ff5c7994cc08 \
                    size    33881

python.versions     37 38 39 310

if {${name} ne ${subport}} {
    depends_build-append    port:py${python.version}-setuptools

    depends_test-append port:py${python.version}-pytest \
                        port:py${python.version}-tox

    test.run        yes
    test.cmd        tox-${python.branch}
    test.env        PYTHONPATH=${worksrcpath}/build/lib

    livecheck.type  none
}
